# Tools/

Reserved per `MASTER_ARCHITECTURE.md` section 8 for standalone developer tooling (e.g. a save-file inspector, a calibration-data viewer) that's neither part of the Python data pipeline nor the C# simulation itself.

Empty for now — nothing here yet has a concrete need. Populated as Phase 1+ work actually surfaces one, rather than speculatively.
