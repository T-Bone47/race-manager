# scripts/

Reserved per `MASTER_ARCHITECTURE.md` section 8 for small automation scripts (e.g. a one-command "run the whole data pipeline + both test suites" helper) that don't belong inside `DataPipeline/` or either C# project.

Empty for now — populated as those needs actually appear, rather than speculatively.
